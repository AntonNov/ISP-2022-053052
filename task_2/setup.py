from setuptools import setup, find_packages

setup(
    name="lab2",
    version=1.0,
    description="Serialization tool",
    author="Anton Novikov",
    packages=find_packages(),
    python_requires=">=3.8",
)
